import { Observable } from 'rxjs';

/**
 * Canvas Warp HtmlElement Event
 */
export interface CanvasViewResizeInterface {

  /**
   * canvas 视图窗口大小改变事件
   */
  canvasViewResizeEvent?: Observable<any>

}