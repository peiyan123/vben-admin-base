import { GuideLineConfig } from '.';
/**
 * GuidelineInterface
 */
export interface GuidelineInterface {
    /**
     * 显示辅助线
     */
    guideLineIsOpen?: boolean
    /**
     * 辅助线配置
     */
    guideLineConfig?: GuideLineConfig
}